<IfModule mod_rewrite.c>  RewriteEngine On  RewriteBase /  # Si le fichier ou le dossier n'existe pas, rediriger vers index.html  RewriteCond %{REQUEST_FILENAME} !-f  RewriteCond %{REQUEST_FILENAME} !-d  RewriteRule ^ index.html [L]</IfModule>